# Canonical CI and local-dev tooling pins for every JDE-Projects Simple X Tool.
# This file is the single source of truth for the ruff and pytest versions. The
# reusable release workflows install from it, and each tool repo copies it as
# requirements-dev.txt so a local run uses exactly what CI uses.
# Dependabot (see ../../.github/dependabot.yml) bumps these pins here. Tool repos
# do not bump them independently; they pick up the change when they refresh their
# copy.
ruff==0.16.4
pytest==9.1.1
